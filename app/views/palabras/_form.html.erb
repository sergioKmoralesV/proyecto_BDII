<%= form_for(@palabra) do |f| %>
  <% if @palabra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@palabra.errors.count, "error") %> prohibited this palabra from being saved:</h2>

      <ul>
      <% @palabra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
